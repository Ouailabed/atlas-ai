node_modules/
.next/
*.log

# Never commit real secrets.
.env*

# ...but DO commit the template, which contains no real values.
# This exception must come after the .env* rule above to take effect.
!.env.local.template
